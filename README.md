# Secure-Login-System
A secure login web application built using Python Flask, SQLite, and bcrypt. The system allows users to register and log in with hashed passwords, manages user sessions, provides logout functionality, and protects against SQL injection using parameterized database queries.
